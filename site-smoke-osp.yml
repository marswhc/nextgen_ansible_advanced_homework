- hosts: workstation
  gather_facts: true
  
  tasks:
  - name: OpenStack servers
    os_server_facts:
     cloud: openstack
     server: 'frontend'
    register: openstack_info
    tags:
      - osp.smoke

#  - name: Curl website
#    uri:
#     url: "http://{{item.public_v4}}"
#     return_content: yes
#    with_items:
#      - "{{ openstack_info.openstack_servers }}"
#    register: webpage
#    tags:
#      - osp.smoke
#  - name: Curl website
#    shell: curl -s "http://{{item.public_v4}}"
#    args:
#      warn: no
#    tags:
#      - osp.smoke
    
  - name: Check webserver for correct response
    uri:
      url: "http://{{openstack_info.public_v4}}/ping"
      return_content: yes
    until: '"alive" in result.content'
    retries: 10
    delay: 1
    register: result
    delegate_to: localhost
